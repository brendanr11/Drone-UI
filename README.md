# Drone-UI — Local Run Guide

This is a React + Vite project extracted from Figma Make.

## Prereqs
- Node.js (recommended: Node 18+)
- npm (comes with Node)

## Install
From the repository root, **cd into the Vite app folder**:

```bash
cd app
npm install
npm run dev

Then open the URL printed in the terminal 
